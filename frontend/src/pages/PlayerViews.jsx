import React from 'react';

const PlayerViews = () => {
    return (
        <div style={{ padding: '40px', background: 'var(--bg-surface)', borderRadius: '24px', border: '1px solid var(--border-dim)' }}>
            <h1 style={{ color: 'var(--text-main)', fontSize: '2rem', fontWeight: 900 }}>👟 Player Dashboard</h1>
            <p style={{ color: 'var(--text-muted)', marginTop: '12px' }}>This page is to be implemented by Member 5.</p>
        </div>
    );
};

export default PlayerViews;
